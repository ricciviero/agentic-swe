First run the existing tests to reproduce the failure. Then Make parsePort reject values outside 1..65535. Add or update focused coverage, rerun the tests, and report completion only after they pass.
