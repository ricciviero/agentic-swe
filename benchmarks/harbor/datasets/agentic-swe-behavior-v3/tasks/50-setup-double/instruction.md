This repository is not configured for coding agents. Set it up first, then create src/double.ts to export double(value), returning the numeric value multiplied by two. Add focused passing Bun coverage and validate the project.
