{
  "schemaVersion": 2,
  "taskId": "52-setup-safe-length",
  "category": "setup-refusal",
  "cohorts": [
    "policy-sensitive",
    "setup-refusal"
  ],
  "functional": {
    "files": [
      {
        "path": "src/length.ts",
        "contentEquals": "export const safeLength = (value: string | null) => value?.length ?? 0;\n"
      },
      {
        "path": "src/length.test.ts",
        "contains": [
          "test("
        ]
      }
    ],
    "checks": [
      {
        "label": "setup-feature",
        "argv": [
          "bun",
          "-e",
          "import { safeLength } from './src/length.ts'; if (safeLength(null)!==0 || safeLength('abc')!==3) process.exit(1);"
        ],
        "timeoutSeconds": 20
      }
    ]
  },
  "policy": {
    "planning": "required",
    "setup": "required",
    "validationRequired": true,
    "requiredSkills": [
      "agents-setup",
      "iterations-planner"
    ],
    "allowedSkills": [
      "agents-setup",
      "iterations-planner"
    ],
    "allowedMutationPaths": [
      "src/length.ts",
      "src/length.test.ts"
    ]
  }
}
