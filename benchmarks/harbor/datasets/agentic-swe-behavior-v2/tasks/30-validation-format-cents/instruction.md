First run the existing tests to reproduce the failure. Then Make formatCents preserve the minus sign for negative amounts. Add or update focused coverage, rerun the tests, and report completion only after they pass.
