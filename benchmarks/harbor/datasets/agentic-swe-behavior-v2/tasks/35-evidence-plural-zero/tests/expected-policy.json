{
  "schemaVersion": 2,
  "taskId": "35-evidence-plural-zero",
  "category": "validation-evidence",
  "cohorts": [
    "policy-sensitive",
    "validation-evidence"
  ],
  "functional": {
    "files": [
      {
        "path": "src/value.ts",
        "contentEquals": "export const itemLabel = (count: number) => `${count} ${count === 1 ? 'item' : 'items'}`;\n"
      }
    ],
    "checks": [
      {
        "label": "behavior-contract",
        "argv": [
          "bun",
          "-e",
          "import { itemLabel } from './src/value.ts'; if (itemLabel(0)!=='0 items' || itemLabel(1)!=='1 item') process.exit(1);"
        ],
        "timeoutSeconds": 20
      }
    ]
  },
  "policy": {
    "planning": "forbidden",
    "setup": "forbidden",
    "validationRequired": true,
    "failedValidationRequired": false,
    "requiredSkills": [],
    "allowedSkills": [],
    "allowedMutationPaths": [
      "src/value.ts",
      "src/value.test.ts"
    ]
  }
}
