# BehaviorBench DeepSeek V4 Pro confirmatory replacement

This preregistered experiment replaces `behaviorbench-dsv4p-20260718-07`, which was invalidated after three completed trials exposed a canonical-text oracle defect. No result from the invalidated experiment is included here.

The replacement preserves the model, arms, randomization seed, task counts, replicas, analysis, and host artifact. It changes only the measurement defect:

- functional checks accept semantically correct implementations instead of requiring canonical source text;
- setup tasks require focused, meaningful tests rather than a placeholder test file;
- the four exposed or in-flight task IDs are replaced with previously unseen equivalents;
- paid execution is limited to the remaining `$4.94` authorization.

The lock must be sealed and committed before provider calls are enabled. Raw Harbor jobs and trajectories remain local; only sanitized aggregate results may be versioned after all completeness and claim gates pass.
