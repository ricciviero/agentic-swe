interface:
  display_name: "Review Animations"
  short_description: "Strict review of animation code"
  default_prompt: "Use $review-animations to review motion code against a high craft bar."

policy:
  allow_implicit_invocation: false
