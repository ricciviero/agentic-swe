interface:
  display_name: "Iterations Planner"
  short_description: "Execute the non-trivial-work planning gate"
  default_prompt: "Use $iterations-planner to create the required local plan for this non-trivial brief."
